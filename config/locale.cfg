// localization tools


// enables localization for a string, so it can be picked up by xgettext
alias gettext [
	result $arg1
]

// shortcut, same as the known c++ macro
alias _ [
	result (gettext $arg1)
]